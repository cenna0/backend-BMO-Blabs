# Copy outside Git to the operator-controlled P9.1 config directory.
P9_ENABLED=true
P9_BIND_HOST=0.0.0.0
P9_BIND_PORT=3010
P9_POSTGRES_DB=bmo
P9_POSTGRES_USER=bmo
P9_POSTGRES_PASSWORD_FILE=/opt/bmo/config/p9.1/postgres-password
P9_DATA_DIR=/opt/bmo/p9.1-data/postgres
P9_BACKUP_DIR=/opt/bmo/p9.1-backups
# Created manually outside the repository in protected secret storage.
# Keep only this path here; never put the material value in this environment file.
# Backup fails closed when the file is missing or empty.
P9_BACKUP_PASSPHRASE_FILE=/opt/bmo/secrets/p9.1/backup/backup-encryption-material-v1
P9_JWT_SECRET=<32-byte-or-longer-runtime-secret>
P9_PAIRING_PEPPER=<32-byte-or-longer-runtime-secret>
