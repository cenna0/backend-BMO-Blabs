# P9.1 Backup Manifest Correction Plan

> **For agentic workers:** Execute this plan inline with test-first checkpoints. Keep all work on `docs/p9.1-production-readiness`; do not touch production.

**Goal:** Make the P9.1 encrypted backup output a verifiable three-file set with a sanitized manifest, then align Windows instructions, readiness metadata, historical pointers, and both verifier regression suites.

**Architecture:** Extract deterministic manifest construction and validation into a side-effect-free TypeScript module. The backup command writes the encrypted artifact and checksum first, validates both, atomically writes the manifest, and only then applies retention. Tests use temporary fixtures and do not invoke Docker, PostgreSQL, GPG, or real secrets.

**Tech Stack:** Node.js 22, TypeScript, Vitest, Prisma backup tooling, PowerShell documentation, Python documentation verifier.

---

### Task 1: Add failing manifest contract tests

**Files:**
- Create: `backend/tests/p9/backup-manifest.unit.test.ts`
- Create: `backend/src/p9/operator/backup-manifest.ts` (only the exported contract surface required by the tests)

- [x] **Step 1: Write tests for valid three-file metadata and fail-closed validation.**

  Cover: artifact filename, byte size, SHA-256, backup identifier, creation status, supported schema, PostgreSQL major, custom encrypted format, forbidden nested secret fields, missing artifact, malformed checksum, checksum mismatch, and manifest/artifact mismatch.

- [x] **Step 2: Run the focused test file and confirm it fails because the manifest module is absent or incomplete.**

  Run: `cd backend && npx vitest run tests/p9/backup-manifest.unit.test.ts`

### Task 2: Implement the deterministic manifest contract

**Files:**
- Modify: `backend/src/p9/operator/backup-manifest.ts`
- Modify: `backend/src/p9/operator/backup.ts`

- [x] **Step 1: Implement pure helpers for SHA-256, checksum parsing, artifact validation, manifest construction, and atomic JSON writing.**

  The manifest must contain only schema version, identifier, UTC timestamp, sanitized database identifier, PostgreSQL major, `custom-encrypted` format, migration state, encrypted filename, exact byte size, SHA-256, and `success` result. Reject missing/empty artifacts, non-custom filenames, malformed or mismatched checksum data, unsupported PostgreSQL versions, unsafe database identifiers, and secret-bearing nested manifest values.

- [x] **Step 2: Integrate the helpers after encryption and checksum creation.**

  Write `<backup-name>.manifest.json` through a unique temporary file, validate it against the finalized encrypted artifact and checksum, rename it atomically, and include the manifest in retention cleanup. Any failure removes only named temporary files and cannot leave a success manifest.

- [x] **Step 3: Run focused tests and typecheck.**

  Run: `cd backend && npx vitest run tests/p9/backup-manifest.unit.test.ts && npm run typecheck`

### Task 3: Align operator and historical documentation

**Files:**
- Modify: `docs/p9/P9.1-WINDOWS-OFF-VPS-BACKUP-GUIDE.md`
- Modify: `docs/NEXT-ACTION.md`
- Modify: `docs/p9/20-backup-restore-migration.md`
- Modify: `docs/p9/24-decision-register.md`
- Modify: `docs/p9/25-unresolved-decisions.md`
- Modify: `docs/p9/P9.1-IMPLEMENTATION-EVIDENCE.md`
- Modify: affected current P9.1 canonical documents as needed for consistency

- [x] **Step 1: Require exact manifest field, type, value, and recursive secret-field validation in the Windows procedure.**

- [x] **Step 2: Correct NEXT-ACTION ordering and stale metadata date.**

- [x] **Step 3: Preserve historical statements while marking them superseded and linking the current Windows operator-PC decision register.**

- [x] **Step 4: State clearly that manifest implementation is complete in source but Windows transfer, decryption, restore, and production verification remain pending.**

### Task 4: Repair verifier regression coverage

**Files:**
- Modify: `scripts/tests/test_verify_backend_mvp_docs.py`
- Modify: `tests/verification/test_verify_backend_mvp_docs.py`
- Modify: `tests/fixtures/docs-verifier/*` only where current-state fixtures require it

- [x] **Step 1: Add or update positive current P9.1/readiness fixtures and negative production/P9.2 fixtures in both suites.**

- [x] **Step 2: Run both suites independently.**

  Run: `python3 -m unittest scripts.tests.test_verify_backend_mvp_docs -v` and `python3 -m unittest tests.verification.test_verify_backend_mvp_docs -v`.

### Task 5: Run complete readiness validation and commit

**Files:**
- All files changed by Tasks 1–4

- [x] **Step 1: Run documentation verifier, Markdown links, backend tests/typecheck, scans, contract hash, and production read-only checks.**

- [x] **Step 2: Review `git diff --check`, changed files, and forbidden generated artifacts.**

- [x] **Step 3: Commit as `fix(p9.1): complete Windows backup manifest readiness`.**

- [x] **Step 4: Push and prove local/remote equality, clean worktree, unchanged main, and unchanged production.**
